# Server
PORT=3001
SSL_PORT=3443
SSL_KEY_PATH=/ssl/key.pem
SSL_CERT_PATH=/ssl/cert.pem

# Logging
LOG_LEVEL=info

# Target host (used by VNC, SSH, and TCP proxy)
TARGET_HOST=localhost

# VNC
VNC_ENABLE=true
VNC_PORT=5900

# SSH Terminal
SSH_PORT=22
SSH_USER=user
SSH_PASS=user
SSH_MAX=5
